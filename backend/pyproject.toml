[project]
name = "restaurant-intel-backend"
version = "0.1.0"
description = "Backend service for Restaurant Intelligence application"
dependencies = [
  "fastapi",
  "uvicorn",
  "alembic",
  "sqlalchemy",
  "psycopg[binary]",
]
